name: win

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:

  test:
    name: Generate Jobs
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Generate Jobs
        run: |
          docker build . -f  3.11\windows\nanoserver-ltsc2022\Dockerfile -t t1
          docker images